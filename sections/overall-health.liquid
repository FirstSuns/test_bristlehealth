<div class="overall_health_wrap">
  <div class="page-width">
      <h1>{{ section.settings.heading }}</h1>
    <div class="overall_health_wrap_inner">
    <div class="left_info">
      <p>{{ section.settings.content }}</p>
    </div>
    <div class="right-info overall_content">
      {% for block in section.blocks %}
         <div class="inner_info">
           <img srcset="
                          {% if block.settings.img.width >= 140 %}{{ block.settings.img | image_url: width: 140 }} 140w,{% endif %}
                          {% if block.settings.img.width >= 518 %}{{ block.settings.img | image_url: width: 518 }} 518w,{% endif %}
                          {% if block.settings.img.width >= 644 %}{{ block.settings.img | image_url: width: 644 }} 644w,{% endif %}
                          {% if block.settings.img.width >= 768 %}{{ block.settings.img | image_url: width: 768 }} 768w,{% endif %}
                          {% if block.settings.img.width >= 830 %}{{ block.settings.img | image_url: width: 830 }} 830w,{% endif %}
                          {% if block.settings.img.width >= 960 %}{{ block.settings.img | image_url: width: 960 }} 960w{% endif %}"
                        src="{{ block.settings.img | image_url: width: 684, height: 684 }}"
                        sizes="(min-width: 1200px) calc((1200px - 19.5rem) / 12), (min-width: 750px) calc((100vw - 16.5rem) / 8), calc((100vw - 8rem) / 5)"
                        alt="{{ block.settings.img.alt | escape }}"
                        loading="lazy" />
           <p>{{ block.settings.title }}</p>
         </div> 
      {% endfor %}
    </div> </div>
  </div>
</div>

{% schema %}
  {
    "name": "Overall Health",
    "settings": [
      {
          "type":"text",
          "id":"heading",
          "label":"Heading"
      },
      {
        "type":"textarea",
        "id":"content",
        "label":"Content"
      }
    ],
     "blocks":[
      {
        "type":"text",
        "name":"Content",
        "settings":[
           {
            "type":"image_picker",
            "id":"img",
            "label":"Image"
          },
          {
            "type":"text",
            "id":"title",
            "label":"Title"
          }
        ]
      }
    ],
    "presets":[
      {
        "name":"Overall Health"
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}